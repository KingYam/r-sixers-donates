{
  "name": "eleventy-site",
  "private": true,
  "version": "1.0.0",
  "author": "Your Name",
  "license": "MIT",
  "description": "Simple eleventy starter site",
  "scripts": {
    "serve": "npx @11ty/eleventy --serve --input=src --output=dist --formats=html,njk,gif,svg,png,jpg,webp,webm,css,js,woff,woff2",
    "build": "npx @11ty/eleventy --input=src --output=dist --formats=html,njk,gif,svg,png,jpg,webp,webm,css,js,woff,woff2"
  },
  "dependencies": {
    "patternomaly": "^1.3.2"
  }
}
